{
  "name": "spareparts-pos",
  "version": "1.0.0",
  "description": "Full-Stack Spare Parts POS System",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "seed": "cd backend && node seed.js",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  }
}
